# Content library files

Place the manager workbook here:

`TRANSFORM_Master_Assessment_Content_Library_V2.xlsx`

Then run `npm run import:library` or import from **Content library** in the console.

Excel and Word files in this folder are gitignored because they contain evaluator keys.
